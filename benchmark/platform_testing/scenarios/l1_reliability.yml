# Level 1: Simple — Reliability Issues (R1-R3 on store_sales via simulated API)
id: l1_reliability
level: 1
seed: 47
source_count: 1
description: "Single source with 3 reliability failures on store_sales"
source_configs:
  - format: csv
    tables: [tpcds_store_sales]
errors:
  - category: reliability
    type: api_failures
    table: tpcds_store_sales
    params: { p_failure: 0.30 }
    expected_detection: "dlq_entry"

  - category: reliability
    type: timeouts
    table: tpcds_store_sales
    params: { p_timeout: 0.10 }
    expected_detection: "timeout_handled"

  - category: reliability
    type: rate_limits
    table: tpcds_store_sales
    params: { p_limited: 0.15 }
    expected_detection: "rate_limit_backoff"
